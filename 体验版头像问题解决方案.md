# 体验版头像打不开问题 - 完整解决方案

## 问题现象
- 开发工具正常显示头像
- **体验版/正式版中头像无法显示**
- 头像路径是 `wxfile://` 格式

---

## 根本原因

### wxfile:// 是临时文件路径
- 小程序中 `open-type="chooseAvatar"` 返回的是临时文件路径 `wxfile://tmp_...`
- 临时文件**仅在当前会话有效**
- 关闭小程序或切换页面后，临时文件会被清理
- 体验版/正式版中打开时，临时文件已经不存在

### 现有代码的问题
```javascript
// app.js saveUserInfo 方法
if (info.avatarUrl.indexOf('wxfile') >= 0) {
  // 上传到服务器...
} catch (e) {
  // 上传失败时，仍然保存 wxfile:// 路径 ❌
  wx.setStorageSync('userInfo', userInfo);
}
```

**问题**：上传失败时保存了临时路径，导致下次打开时头像失效。

---

## 解决方案（已修复）✅

### 修改1：上传失败时不保存临时路径

```javascript
// app.js saveUserInfo
catch (e) {
  // 如果是头像上传失败
  if (info.avatarUrl && info.avatarUrl.indexOf('wxfile') >= 0) {
    wx.showToast({ 
      title: '头像上传失败，请检查网络', 
      icon: 'none' 
    });
    // ✅ 不保存临时路径，保持原有头像
    delete info.avatarUrl;
    const fallbackInfo = Object.assign({}, this.globalData.userInfo, info);
    this.globalData.userInfo = fallbackInfo;
    wx.setStorageSync('userInfo', fallbackInfo);
  }
}
```

### 修改2：添加详细日志和用户提示

```javascript
// 上传时显示 loading
wx.showLoading({ title: '上传头像中...', mask: true });

// 详细日志
console.log('[头像上传] 开始上传临时文件:', info.avatarUrl);
console.log('[头像上传] 上传成功:', data.url);
console.error('[头像上传] 上传失败:', err);

// 上传成功后隐藏
wx.hideLoading();
```

---

## 备选方案：使用云存储

如果服务器不稳定或没有上传接口，可以使用**小程序云存储**：

### 优点
- 无需配置服务器和域名
- 自动处理图片CDN加速
- 云存储URL永久有效
- 免费额度足够小程序使用

### 实现步骤

#### 1. 开通云开发（如果未开通）
```javascript
// app.js onLaunch
wx.cloud.init({ 
  env: 'your-env-id',  // 在云开发控制台获取
  traceUser: true 
});
```

#### 2. 修改 saveUserInfo 上传到云存储

```javascript
// app.js
async saveUserInfo(info) {
  const userInfo = Object.assign({}, this.globalData.userInfo, info);

  try {
    // 如果头像是本地临时路径，上传到云存储
    if (info.avatarUrl && (info.avatarUrl.indexOf('tmp') >= 0 || info.avatarUrl.indexOf('wxfile') >= 0)) {
      wx.showLoading({ title: '上传头像中...', mask: true });
      
      // 生成唯一文件名
      const timestamp = Date.now();
      const random = Math.random().toString(36).substr(2, 6);
      const cloudPath = `avatars/${userInfo.userId}_${timestamp}_${random}.jpg`;
      
      // 上传到云存储
      const uploadRes = await wx.cloud.uploadFile({
        cloudPath: cloudPath,
        filePath: info.avatarUrl
      });
      
      wx.hideLoading();
      console.log('[头像上传] 云存储上传成功:', uploadRes.fileID);
      
      // 使用云存储 fileID（无需域名配置）
      userInfo.avatarUrl = uploadRes.fileID;
      
      // 或获取临时下载链接（有效期7天，适合分享）
      // const tempURLRes = await wx.cloud.getTempFileURL({
      //   fileList: [uploadRes.fileID]
      // });
      // userInfo.avatarUrl = tempURLRes.fileList[0].tempFileURL;
    }

    this.globalData.userInfo = userInfo;
    wx.setStorageSync('userInfo', userInfo);

    // 同步到后端
    await api.callFunction('login', {
      action: 'updateProfile',
      nickname: userInfo.nickname,
      avatarUrl: userInfo.avatarUrl
    });
    
  } catch (e) {
    wx.hideLoading();
    console.error('[头像上传] 失败:', e);
    wx.showToast({ title: '头像上传失败', icon: 'none' });
    
    // 上传失败不保存临时路径
    if (info.avatarUrl && info.avatarUrl.indexOf('wxfile') >= 0) {
      delete info.avatarUrl;
    }
    const fallbackInfo = Object.assign({}, this.globalData.userInfo, info);
    this.globalData.userInfo = fallbackInfo;
    wx.setStorageSync('userInfo', fallbackInfo);
  }
}
```

#### 3. 云存储 URL 格式

```javascript
// fileID 格式（推荐，永久有效）
cloud://env-id.xxxx/avatars/user123_1234567890_abc123.jpg

// 临时下载链接格式（有效期7天）
https://xxx.tcb.qcloud.la/avatars/user123_1234567890_abc123.jpg?sign=...
```

#### 4. 配置云存储权限（可选）

在云开发控制台 → 存储 → 权限设置：
```json
{
  "read": true,   // 所有人可读
  "write": false  // 仅创建者可写
}
```

---

## 方案对比

| 方案 | 优点 | 缺点 | 推荐 |
|------|------|------|------|
| **自建服务器上传** | 数据完全可控 | 需要服务器开发和维护 | ⭐⭐⭐ |
| **云存储 fileID** | 永久有效、无需域名配置 | 依赖云开发 | ⭐⭐⭐⭐⭐ |
| **云存储临时链接** | 可分享给外部 | 7天过期需要刷新 | ⭐⭐ |

---

## 验证修复

### 1. 测试上传成功场景
```
1. 点击头像选择器
2. 选择图片
3. 看到 "上传头像中..." loading
4. loading 消失，头像显示正常
5. 重新打开小程序，头像仍然正常显示
6. 头像路径是 https://... 而非 wxfile://
```

### 2. 测试上传失败场景
```
1. 断开网络
2. 点击头像选择器，选择图片
3. 看到 "头像上传失败，请检查网络" 提示
4. 头像保持原样（未改变）
5. 本地不会保存 wxfile:// 路径
```

### 3. 查看日志
```
[头像上传] 开始上传临时文件: wxfile://tmp_...
[头像上传] 上传成功: /uploads/avatars/xxx.jpg
[头像上传] 使用服务器URL: https://meetpoint.top/uploads/avatars/xxx.jpg
[用户信息] 保存成功
```

---

## 常见问题

### Q1: 为什么开发工具正常，体验版不正常？
**A**: 开发工具中临时文件不会过期，体验版关闭后临时文件被清理。

### Q2: 服务器上传接口需要什么功能？
**A**: 接收 `multipart/form-data` 文件上传，保存到服务器静态目录，返回公网访问URL。

示例 Node.js 接口：
```javascript
// Express + multer
app.post('/api/uploadAvatar', upload.single('file'), (req, res) => {
  const fileUrl = '/uploads/avatars/' + req.file.filename;
  res.json({ ok: true, url: fileUrl });
});
```

### Q3: 云存储 fileID 和临时链接哪个好？
**A**: 
- **fileID** - 永久有效，推荐用于小程序内展示
- **临时链接** - 7天过期，推荐用于分享给外部（如生成海报）

### Q4: 头像上传失败率高怎么办？
**A**: 
1. 添加重试机制（最多3次）
2. 使用图片压缩减小文件大小
3. 使用云存储代替自建服务器

---

## 总结

✅ **已修复**：头像上传失败时不再保存临时路径  
✅ **已添加**：详细日志和用户提示  
✅ **推荐**：使用云存储 fileID 方案，永久有效且无需域名配置  

**关键点**：永远不要保存 `wxfile://` 路径到持久化存储！
