# 真机上看不见好友头像 - 问题排查指南

## 问题现象
- 开发工具上能正常显示好友头像
- **真机上只看到绿色圆圈，里面是纯灰色**
- 自己的头像正常显示

---

## 根本原因分析

### 代码逻辑确认 ✓
1. **Canvas合成逻辑正确** - `_buildFriendAvatarMarker` 已实现
2. **网络图片下载** - 使用 `wx.downloadFile` 下载好友头像
3. **缓存机制** - `_friendMarkerCache` 避免重复构建
4. **触发时机** - 在 `_refreshMeeting` 中检测到好友有头像URL时自动触发

### 最可能的原因：**网络头像下载失败**

真机上 `wx.downloadFile` **必须配置合法域名白名单**，否则会被拦截！

---

## 排查步骤

### 1. 查看真机控制台日志

在真机上打开调试模式（微信开发者工具 - 预览 - 开启调试），查看以下关键日志：

```
[好友头像] 开始构建: <userId> <avatarUrl> <borderColor>
[好友头像] 网络图片，开始下载: <avatarUrl>
[好友头像] 下载失败: <userId> <avatarUrl> <错误信息>
```

**如果看到 "下载失败"，错误信息可能是：**
- `request:fail url not in domain list` - **域名未配置**
- `request:fail net::ERR_CONNECTION_REFUSED` - 网络不可达
- `request:fail timeout` - 超时

---

### 2. 检查小程序合法域名配置

#### 在微信公众平台配置：

1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. 进入【开发】→【开发管理】→【开发设置】
3. 找到 **"服务器域名"** 配置
4. 在 **"downloadFile 合法域名"** 中添加：

```
https://thirdwx.qlogo.cn
https://wx.qlogo.cn
```

这两个域名是微信用户头像的CDN域名。

> **注意**：域名配置需要以下条件：
> - 必须是 HTTPS
> - 需要ICP备案
> - 每月最多修改5次
> - 配置后立即生效（可能需要1-2分钟）

---

### 3. 确认头像URL格式

检查好友头像URL是否符合以下格式：

```javascript
// 微信头像常见格式（正常）
https://thirdwx.qlogo.cn/mmopen/...
https://wx.qlogo.cn/mmopen/...

// 本地临时文件（正常）
wxfile://tmp_...

// 自定义服务器（需要配置域名）
https://your-server.com/avatars/...
```

---

### 4. 检查 Canvas API 兼容性

真机上 Canvas 2D API 可能有兼容性问题。已添加的错误日志：

```javascript
[Canvas合成] 底图加载失败: <path>
[Canvas合成] 头像加载失败: <path>
[Canvas合成] 导出失败: <error>
```

如果看到这些错误，说明Canvas合成失败。

---

## 解决方案

### 方案1：配置合法域名（推荐）✅

在微信公众平台配置 `downloadFile` 合法域名：
```
https://thirdwx.qlogo.cn
https://wx.qlogo.cn
```

配置后立即重新打开小程序测试。

---

### 方案2：开发期间临时绕过域名检查

**仅用于开发测试，不能用于生产环境！**

在手机微信中：
1. 扫描开发工具生成的预览二维码
2. 点击右上角 **"..."**
3. 打开 **"开启调试"**

开启调试后会绕过域名检查，但**正式发布必须配置合法域名**。

---

### 方案3：修改代码使用云存储（备选）

如果无法配置CDN域名（如企业账号无权限），可将头像转存到小程序云存储：

```javascript
// 1. 下载头像到本地
wx.downloadFile({
  url: avatarUrl,
  success: async (res) => {
    // 2. 上传到云存储
    const cloudPath = 'avatars/' + userId + '.jpg';
    await wx.cloud.uploadFile({
      cloudPath,
      filePath: res.tempFilePath
    });
    // 3. 获取云存储URL（无需域名配置）
    const fileID = 'cloud://...' + cloudPath;
    // 4. 使用云存储URL合成头像
    doComposite(fileID);
  }
});
```

---

## 验证修复

配置域名后，在真机上应该能看到：

```
[好友头像] 开始构建: ...
[好友头像] 网络图片，开始下载: https://thirdwx.qlogo.cn/...
[好友头像] 下载成功: 200 wxfile://tmp_...
[好友头像] 开始Canvas合成: wxfile://tmp_...
[好友头像] 底图生成成功(88px): ...
[Canvas合成] 图片加载进度: 1 /2
[Canvas合成] 图片加载进度: 2 /2
[Canvas合成] 导出成功: wxfile://tmp_...
[好友头像] 合成成功(normal): wxfile://tmp_...
```

地图上好友marker显示为：**绿色圆圈 + 好友真实头像**

---

## 常见错误排查

### 错误1：`url not in domain list`
**原因**：域名未配置  
**解决**：在微信公众平台添加 downloadFile 合法域名

### 错误2：头像URL是 `http://`（非HTTPS）
**原因**：小程序只允许HTTPS  
**解决**：将头像URL改为HTTPS或转存云存储

### 错误3：Canvas加载失败（onerror触发）
**原因**：真机Canvas不支持某些图片格式/路径  
**解决**：确保使用 `wxfile://` 临时文件路径，不要直接用网络URL

### 错误4：只有部分好友头像失败
**原因**：个别好友头像URL格式特殊或图片损坏  
**解决**：为这些用户使用默认头像

---

## 代码已添加的日志

为了方便排查，代码中已添加详细日志：

1. `_buildFriendAvatarMarker` - 构建流程跟踪
2. `wx.downloadFile` - 下载成功/失败
3. `_compositeOne` - Canvas合成各阶段
4. `onload/onerror` - 图片加载事件

**真机调试时务必查看这些日志！**

---

## 总结

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 真机看不见好友头像 | 下载域名未配置 | 配置 `downloadFile` 合法域名 ✅ |
| 开发工具正常，真机异常 | 开发工具关闭了 urlCheck | 配置真实域名白名单 |
| 自己头像正常，好友不正常 | 自己头像在 onLoad 就下载了 | 好友头像也需要下载，域名配置一致 |

**最快的解决方法：在微信公众平台添加 `https://thirdwx.qlogo.cn` 和 `https://wx.qlogo.cn` 到 downloadFile 合法域名！**
